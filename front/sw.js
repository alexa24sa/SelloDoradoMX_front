// Skeleton del Service Worker para la PWA

self.addEventListener('install', (event) => {
    // Instalación y caché inicial
});

self.addEventListener('fetch', (event) => {
    // Intercepción de peticiones (estrategias de caché)
});